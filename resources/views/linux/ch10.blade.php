<h3>Chapter 10:</h3>
<hr />


<p><strong>Quiz</strong></p> 
<ul>
	<li>This stream is the output of a command operating normally: stdout</li>
	<li>Many commands that read text files will also read from this stream: stdin</li>
	<li>If you want to overwrite a file by redirecting the output of a command, you can use: ></li>
	<li>To redirect errors that are output by a command, you can use: 2></li>
	<li>Two or more commands combined with the "|" between them form a: pipeline</li>
	<li>Which two symbols can effectively redirect stdin to a command?: < and |</li>
	<li>If you want to append a file with the normal output of a command, you can use: >></li>
	<li>To send the normal and error output of a command to a single file, you can use: &amp;></li>
	<li>Which of the following commands will take the standard output of the ls and put it into the /tmp/output.txt file: ls > /tmp/output.txt</li>
	<li>Which of the following commands will take the standard error of the ls command and put it into the /tmp/output.txt file: ls 2> /tmp/output.txt</li>
</ul>