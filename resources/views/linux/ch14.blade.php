<h3>Chapter 14: Filesystem Links</h3>
<hr />


<p><strong>Quiz</strong></p> 
<ul>
    <li>Which are valid link types in Linux? (choose two): Hard links, Soft links</li>
    <li>Which of the following commands will create a soft link to /tmp/test named /tmp/data?: <code>ln -s /tmp/test /tmp/data</code></li>
    <li>Which of the following commands will create a hard link to /tmp/test named /tmp/data?: <code>ln /tmp/test /tmp/data</code></li>
    <li>If you have a file named /tmp/hosts pointing to a file named /etc/hosts and the /etc/hosts file is deleted, then which of the following statements is true?: The /tmp/hosts file now points to nothing</li>
    <li>Which value represents the hard link count in the following output of the ls -li command: 87589 -rw-r--r--. 2 root root 83 Mar 4 22:45 myhosts: 2</li>
    <li>Which type of link can be made to directories, hard or soft?: soft</li>
    <li>Which type of link is easier to visually "see", hard or soft?: soft</li>
    <li>Which type of link is indistinguishable by programs from regular files, hard or soft?: hard</li>
    <li>When viewing a file with the ls -l command, which character represents a file type of soft link?: l</li>
    <li>Which value represents the inode number in the following output of the ls -li command: 87589 -rw-r--r--. 2 root root 83 Mar 4 22:45 myhosts: 87589</li>
    <li>A soft link is also called a _____ link.: symbolic link</li>
    <li>Which option to the ls command displays the inode numbers of files?: <code>-i</code></li>
    <li>When viewing a file with the ls -l command, which character represents a file type of soft link?: l</li>
    <li>What option to the find command allows you to search for files by inode numbers?: <code>-inum</code></li>
</ul>