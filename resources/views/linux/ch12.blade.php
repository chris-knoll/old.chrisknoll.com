<h3>Chapter 12: Archiving</h3>
<hr />


<p><strong>Quiz</strong></p> 
<ul>
	<li>Which option to the zip command can be used to recursively compress multiple directories?: <code>-r</code></li>
	<li>The unzip command can be used without options to extract files from a zip archive: True</li>
	<li>Which option to the bzip2 command can be used for recursive compression?: bzip2 doesn't support recursive compression</li>
	<li>gzip and bzip2 use the same compression algorithm: False</li>
	<li>By default, tar will attempt to extract an archive...: Into the working directory</li>
	<li>The cpio command has four modes of operation: False</li>
	<li>Which of the following options puts the cpio command into copy-in mode?: <code>-i</code></li>
	<li>Which of the following options allows the cpio command to overwrite existing files?: <code>-u</code></li>
	<li>When copying entire devices using the dd command, you must specify: input file and output file only</li>
	<li>Which of the following is not a valid argument for the dd command?: in</li>
	<li>Which option to the gzip command can be used to retain the uncompressed files when creating an archive?: <code>-c</code></li>
	<li>It is not possible to recursively compress files within directories with gzip.: False</li>
	<li>The tar command supports which of the following styles of option(s)?: BSD, GNU, and Unix</li>
	<li>Which of the following options puts the cpio command in copy-out mode?: <code>-o</code></li>
	<li>The cpio command will preserve metadata when copying files.: True</li>
	<li>The dd command can be used to copy entire partitions.: True</li>
</ul>