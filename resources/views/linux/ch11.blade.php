<h3>Chapter 11: Managing Processes</h3>
<hr />


<p><strong>Quiz</strong></p> 
<ul>
	<li>The main advantage of running a process in the background is: You can continue to use the shell that started the process</li>
	<li>Using the kill command always terminates processes: false</li>
	<li>If you want a background process to keep running after log out, you can use: <code>nohup</code></li>
	<li>To view statistics on memory availability, you can use: <code>nohup</code></li>
	<li>Which of the following is not a valid way to send the "force kill" signal to a process?: <code>kill -FORCE PID</code></li>
	<li>To execute a command with the highest possible priority, which value do you pass to the nice command?: -20</li>
	<li>The _____ command will list the commands that are running in your terminal: <code>ps</code></li>
	<li>To send a paused process to the background, use the _____ command: <code>bg</code></li>
	<li>To set a priority value lower than 0, you must log in as which user?: The root user</li>
	<li>What character do you place between commands to execute multiple commands on a single command line?: <code>;</code></li>
	<li>To view all processes on the system, you can execute: <code>ps -e</code></li>
	<li>A popular program for monitoring running processes in real-time is: <code>top</code></li>
	<li>What command can be used as a prefix to another command to run it at an altered priority?: <code>nice</code></li>
	<li>A signal is: A technique to tell a process to take some sort of action</li>
	<li>To execute a command with the lowest possible priority, which value do you pass to the nice command?: 19</li>
	<li>To see how long the system has been running, you can use the _____ command: <code>uptime</code></li>
	<li>What option to the kill command will list the signals for the system?: <code>-l</code></li>
</ul>